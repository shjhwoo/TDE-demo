package data

var QueryList = []string{
	`SELECT
				C.*,
				CASE
					WHEN CUSTLSTDATE = ''
					OR CUSTLSTDATE IS NULL THEN CUSTREGDATE
					ELSE CUSTLSTDATE
				END AS ORDERDATE
			FROM
				h00000.tcustomerpersonal C
			WHERE
				C.DISCD <> 1
				AND C.ORGID = 90
				AND ((CUSTDOB LIKE '%01042773114%'
					OR UPPER(CUSTNO) LIKE '%01042773114%'
						OR CUSTCELL1 LIKE '%01042773114%'
						OR CUSTCELL2 LIKE '%01042773114%'))
			ORDER BY
				CUSTNAME ASC,
				ORDERDATE DESC`,
	`SELECT
	CUSTNAME,
	SURNAME,
	CUSTNO,
	CUSTJN,
	CUSTDOB,
	CUSTADDR11,
	CUSTADDR12,
	CUSTPIC,
	CUSTCELL1,
	CUSTPHONE1,
	CUSTLVL,
	CUSTGENDER,
	CUSTMEMO,
	CUSTFSTDATE,
	IFNULL(PAID, 0) PAYMENTAMT,
	P.PERSONALINFO,
	P.REFERRAL,
	S.SCHEDULEID,
	S.ORGID,
	S.CUSTOMERID,
	S.RESVTIME,
	S.RESVDATE,
	S.SCHEDULEDATE,
	S.SCHEDULETIME,
	S.SCHEDULESTATUS,
	S.SCHDOCTOR,
	S.SCHNURSE,
	S.PHYSICALEMPL,
	S.CONSULTTIME,
	S.SVCAREA,
	S.VISITTYPE,
	S.CONSULTNOTE,
	S.PROGRESSNOTE,
	S.RESVMEMO,
	S.NURSEMEMO,
	S.BOOKMARK,
	S.INSTYPE,
	S.DISCD,
	S.CRTIME,
	S.CONDITION1,
	S.CONDITION2,
	S.RESVCOUNT,
	S.RESVCFM,
	S.RESVTMO,
	S.RESVTDY,
	S.HISTMEMO,
	S.EXTRACOST,
	S.RESVEMPLID,
	S.EXRECALL,
	S.TRPLAN,
	S.ASSESSMENT,
	S.PAYMENTTYPE,
	S.MEDCOST,
	S.PREGNANT,
	S.MIG,
	S.NOCON,
	IFNULL(S.NOCALC, 0) AS NOCALC,
	S.MODHIST,
	S.SUMMARY,
	S.TREATMENTROOM,
	S.SVCAREA2,
	S.REGTYPE,
	S.SCHTYPE,
	S.SEQNO,
	S.COLOR,
	S.ROOMNO,
	S.DURATION,
	S.TREATMENTGROUP,
	S.INTIME,
	S.GENTASK,
	S.SELFCHCKSTATUS,
	IFNULL(LIABILITYAMT, 0) LIABILITYAMT,
	IFNULL(CLAIMAMT, 0) CLAIMAMT,
	IFNULL(AIDAMT1, 0) AIDAMT1,
	IFNULL(NONINSAMT, 0) NONINSAMT,
	IFNULL(DISCOUNTAMT, 0) DISCOUNTAMT,
	IFNULL(CARINSAMT, 0) CARINSAMT,
	IFNULL(LOSSAMT, 0) LOSSAMT,
	PAID,
	IFNULL(TAXDCAMT, 0) TAXDCAMT,
	IFNULL(LTCLAIMAMT, 0) LTCLAIMAMT,
	IFNULL(LTCHARGEAMT, 0) LTCHARGEAMT,
	IFNULL(BOHUNAMT, 0) BOHUNAMT,
	IFNULL(BOHUN100AMT, 0) BOHUN100AMT,
	IFNULL(BOHUNLTCLAIMAMT, 0) BOHUNLTCLAIMAMT
FROM
	h00000.tcustomerschedule S
INNER JOIN h00000.tcustomerpersonal P ON
	S.CUSTOMERID = P.CUSTOMERID
LEFT JOIN h00000.tsalestatement M ON
	S.SCHEDULEID = M.SCHEDULEID
LEFT JOIN (
	SELECT
		X.SCHEDULEID,
		SUM(IF(PAYMENTCODE LIKE 'R%' OR PAYMENTCODE LIKE 'D%', -1 * PAYMENTAMT, PAYMENTAMT)) AS PAID
	FROM
		h00000.tpayment X
	WHERE
		ORDERDATE = '20231101' 
		AND ORGID = 1237 
	GROUP BY
		X.SCHEDULEID 
               ) N ON
	N.SCHEDULEID = S.SCHEDULEID
WHERE
	S.ORGID = 1237 
	AND S.SCHEDULEDATE = '20231101'
	AND S.DISCD <> 1
ORDER BY
	S.SCHEDULETIME`,
}
