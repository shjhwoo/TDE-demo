# TDE 핵심

1. 어플리케이션 코드는 수정 필요 X
2. 키를 어떻게 관리를 할것인가

# Data at Rest Encryption

# Key Management Plugin

암호화 키 관리를 어떻게 할지 결정하는 플러그인

# InnoDB Tablespace Encryption

실제 데이터 저장되는 .ibd 파일 암호화 설정

- 제공하는 기능들

- 활용 가능한 방향들 + 데모

- 레플리케이션 + 갈레라

* 암호화 키의 동기화, 운영상 모든노드가 동일한 키 파일을 바라보게 구성
* 데이터 파일뿐 아니라 네트워크를 타고 넘어가는 Binary log도 암호화해야 함..

- 적용전후 오버헤드
  sysbench



⚠️ 주의사항 (오버헤드 발생)
이 과정은 테이블의 모든 데이터를 읽고 쓰는 I/O 집약적인 작업입니다.

시간 소요: 테이블 크기가 클수록 완료되는 데 시간이 오래 걸립니다.

디스크 공간: 일시적으로 기존 파일과 새 파일이 모두 존재해야 하므로, 테이블 크기의 약 2배에 해당하는 디스크 공간이 필요합니다.

락(Lock): ALTER TABLE이 실행되는 동안 해당 테이블에는 **배타적 락(Exclusive Lock)**이 걸려 테이블을 사용할 수 없는 다운타임이 발생할 수 있습니다.

따라서 실제 운영 환경에서 대용량 테이블에 암호화를 적용할 때는 서비스 영향도를 최소화하기 위한 별도의 전략(예: 온라인 DDL 툴 사용 등)을 고려해야 합니다.

* 이미 암호화된 테이블을 복호화 하는 건 단순히 ALTER TABLE로는 불가하다 .
* 그런데 비 암호화된 테이블을 암호화 하는건
ALTER TABLE {테이블이름} ENCRYPTED = 'YES'; 로도 충분히 가능하다.

```

---

DevOps 영역의 업무를 맡게 되셔서 부담이 있으시겠지만, TDE(Transparent Data Encryption)는 개념을 잡고 나면 설정 자체는 마법처럼 복잡하지는 않습니다. **핵심은 "애플리케이션 코드는 수정할 필요가 없다(Transparent)"는 점과 "키 관리(Key Management)"**입니다.

11/26(수) 중간 점검과 12/4(목) 최종 발표까지 남은 시간을 고려하여, **단계별 리서치 및 준비 로드맵**을 정리해 드립니다.

---

### 📅 전체 일정 로드맵

- **Today ~ 11/24(일):** TDE 기본 개념 학습, 로컬(Docker)에서 단일 DB 암호화 성공 및 검증 (중간 점검 준비 완료)
- **11/25(화):** 중간 점검용 장표(1~2장) 및 데모 시나리오 정리
- **11/26(수) 09:30:** **[중간 점검 미팅]**
- **11/27(목) ~ 11/29(토):** 리플리케이션/갈레라 환경에서의 TDE 구성 테스트
- **11/30(일) ~ 12/1(월):** `sysbench` 등을 이용한 성능(오버헤드) 비교 테스트
- **12/2(화) ~ 12/3(수):** 최종 발표 자료 작성 및 데모 리허설
- **12/4(목) ~ 12/5(금):** **[최종 발표]**

---

### 🚀 Phase 1. 기본기 & 단일 노드 데모 (지금 ~ 11/26)

이 단계의 목표는 **"TDE를 켰더니 DB 파일이 진짜 암호화되더라, 하지만 Select는 잘 되더라"**를 눈으로 확인하는 것입니다.

#### 1. 리서치 핵심 키워드

MariaDB 공식 문서를 볼 때 다음 키워드 위주로 보세요.

- **Data at Rest Encryption:** TDE의 정식 명칭입니다. (저장된 데이터 암호화)
- **Key Management Plugin:** 암호화 키를 어떻게 관리할지 결정하는 플러그인입니다.
  - _데모용:_ `File Key Management Plugin` (가장 쉽고 직관적, 파일에 키 저장)
  - _실무용:_ AWS KMS Plugin 등 (중간 점검 때는 File 방식으로 먼저 보여주세요)
- **InnoDB Tablespace Encryption:** 실제 데이터가 저장되는 `.ibd` 파일 암호화 설정입니다.

#### 2. 데모 시나리오 (검증 방법)

가장 확실한 시각적 효과는 **`strings` 명령어**나 **`hexdump`**를 사용하는 것입니다.

1.  **TDE 적용 전:** 테이블에 "MySecretCreditCard"라는 데이터를 넣는다 -> DB 데이터 파일(`table.ibd`)을 `strings` 명령어로 열어본다 -> "MySecretCreditCard"가 **그대로 보인다.** (충격 요법)
2.  **TDE 적용 후:** 설정 파일(`my.cnf`)에 암호화 옵션을 켠다 -> 똑같이 데이터를 넣거나 기존 테이블을 암호화한다 -> 파일 시스템에서 `strings`로 열어본다 -> **데이터가 깨져서 안 보인다.**
3.  **SQL 확인:** DB 접속해서 `SELECT` 하면 **잘 보인다.** (Transparent 증명)

#### 3. 중간 점검 때 보여줄 것

> "현재 로컬 Docker 환경에서 File Key Management 플러그인을 사용해 InnoDB 테이블스페이스 암호화에 성공했습니다. OS 레벨에서는 데이터 식별이 불가능함을 확인했습니다."

---

### 🛠️ Phase 2. 심화 & 운영 환경 고려 (11/27 ~ 12/1)

운영 환경(리플리케이션/갈레라)과 성능 이슈는 이 단계에서 다룹니다.

#### 1. 리플리케이션 & 갈레라 클러스터 고려 사항

이 부분이 제일 까다롭습니다. 핵심은 **"암호화 키의 동기화"**입니다.

- **독립적 키 관리:** 원칙적으로 각 노드는 독립적입니다. Master와 Slave가 서로 다른 키를 가질 수도 있지만, 운영상 **모든 노드가 동일한 키 파일(혹은 KMS 경로)을 바라보도록 구성**하는 것이 일반적입니다.
- **Binlog 암호화:** 리플리케이션을 한다면 데이터 파일뿐만 아니라 네트워크를 타고 넘어가는 `Binary Log`도 암호화해야 진정한 보안이 완성됩니다. (`encrypt_binlog` 옵션 확인)
- **Galera SST:** 새로운 노드가 합류할 때 데이터를 통째로 복사(SST)하는데, 이 과정에서 암호화된 파일이 넘어가므로 전송 구간(SSL) 보안도 함께 언급해주면 좋습니다.

#### 2. 성능(Overhead) 측정

TDE는 CPU를 사용하여 데이터를 암호화/복호화하므로 CPU 사용량이 늘어납니다.

- **툴:** `sysbench` (가장 대중적인 DB 벤치마킹 툴)
- **비교:** TDE Off 상태 vs TDE On 상태에서의 TPS(초당 트랜잭션) 및 Latency 비교.
- **예상 결과:** 보통 3~10% 내외의 오버헤드가 발생한다고 알려져 있습니다. 이 수치를 직접 측정해서 보여주면 베스트입니다.

---

### 📝 발표 자료 구성 (Tip)

발표는 개발자/엔지니어를 대상으로 하므로 다음과 같은 흐름을 추천합니다.

1.  **Why:** 왜 TDE인가? (디스크 탈취 시 데이터 보호, 컴플라이언스 대응)
2.  **How it works:** 아키텍처 다이어그램 (애플리케이션 <-> MariaDB(메모리는 평문) <-> [암호화/복호화] <-> 디스크(암호문))
3.  **Demo:** (Phase 1에서 준비한 터미널 화면 캡처 또는 라이브 데모)
4.  **Operational Impact:** 리플리케이션/갈레라 구조에서의 키 관리 전략, 성능 벤치마크 결과.
5.  **Risk & Constraint:** 키 파일을 잃어버리면 데이터 영구 손실(가장 중요), CPU 사용량 증가.

---

### 💡 지금 바로 해야 할 First Step

**Docker로 MariaDB 띄우고 `File Key Management Plugin` 설정해보기**입니다. 이것만 성공하면 중간 점검은 무난히 통과합니다.

제가 **"MariaDB TDE 설정을 위한 `my.cnf` 예시와 Docker Compose 파일"**을 작성해 드릴 수 있습니다. 이걸로 바로 실습을 시작해 보시겠습니까?

---

레거시 DB를 암호화해야 하는 DBA 입장에서 **가장 궁금해야 할 질문**은 **'서비스 영향도'와 '운영 지속 가능성'**에 초점을 맞춘 다음 세 가지입니다.

---

## 1. 🚨 서비스 영향도: 다운타임은 얼마나 필요한가?

레거시 서비스는 일반적으로 24/7 가동되어야 하므로, 암호화 과정에서 발생하는 **서비스 중단 시간(다운타임)**이 가장 민감한 문제입니다.

- **TDE 적용 시간:** `ALTER TABLE ... ENCRYPTED=YES` 명령을 실행할 때, **테이블 락(Lock)**이 걸리는 시간은 얼마나 되며, 이를 **온라인 DDL 방식**으로 최소화할 수 있는가?
  - _관련 궁금증:_ 가장 큰 테이블 3개의 **암호화 전환 예상 시간**은 얼마인가? (I/O 성능 분석 기반)
- **성능 저하:** 암호화 프로세스(디스크 쓰기/읽기) 진행 중 **DB 서버의 CPU 및 I/O 부하**가 서비스 임계치를 넘지 않는가?

---

## 2. 💸 비용 및 자원 효율성: 어떤 자원을 얼마나 써야 하는가?

DBA는 단순한 도입 비용 외에도 운영에 필요한 자원 증가분을 궁금해해야 합니다.

- **성능 오버헤드:** TDE 적용 후 암호화/복호화 과정으로 인해 **평균 쿼리 응답 시간(Latency)**이 몇 % 증가하는가? (특히 CPU 사용량)
- **디스크 공간:** 암호화 전환 과정 중 기존 파일과 새로운 암호화 파일이 일시적으로 존재해야 하는데, 필요한 **최대 디스크 공간**은 테이블 크기의 몇 배인가?
- **키 관리 비용:** 키 관리 솔루션(예: KMIP 서버, AWS KMS) 도입 시 발생하는 **직접 비용 및 유지보수 인력 비용**은 얼마인가?

---

## 3. 🛡️ 운영 및 재해 복구: 보안과 관리는 어떻게 하는가?

암호화는 데이터를 안전하게 보호하는 동시에, **데이터 유실 위험을 증폭**시키므로 복구 계획이 필수적입니다.

- **백업/복구 절차 변경:** 암호화된 테이블이 포함된 백업을 복구할 때 **암호화 키**가 어떻게 필요한가? 백업/복구 시나리오가 얼마나 복잡해지는가?
- **키 교체(Key Rotation) 계획:** 마스터 키를 얼마나 **자주, 그리고 어떻게** 교체해야 하는가? 키 교체 작업은 서비스에 영향을 주지 않는가?
- **접근 통제:** 암호화 키 접근 권한을 가진 계정/사용자를 **어떻게 최소화하고 관리**할 것인가?

> **요약:** 레거시 DB 암호화는 단순히 `ENCRYPTED=YES`를 실행하는 문제가 아니라, **운영 중인 시스템을 멈추지 않고, 암호화 키를 안전하게 관리하며, 만약의 사태에 대비한 완벽한 복구 계획**을 세우는 것이 핵심입니다.
