회사에서 **부분 암호화**를 선호하는 요구사항을 충족시키면서 **기존 레거시 데이터**를 안전하게 마이그레이션하려면, **TDE(테이블 레벨)**와 **컬럼 레벨 암호화** 중 어떤 것을 선택하느냐에 따라 시간과 비용이 극단적으로 달라집니다.

다음은 두 가지 옵션을 비교하고, 레거시 DB 암호화에 필요한 시간 및 비용 효율성을 평가하는 요소들입니다.

---

## 1. 부분 암호화 옵션 선택 및 효율성

부분 암호화 요구사항은 두 가지 방식으로 충족될 수 있으며, **TDE(테이블 레벨)**가 레거시 마이그레이션에 압도적으로 효율적입니다.

### A. TDE: 테이블 단위 부분 암호화 (고효율 경로)

- **방식:** 민감한 정보가 담긴 특정 테이블만 **`ALTER TABLE ... ENCRYPTED=YES`** 명령어를 사용하여 암호화합니다.
- **시간 및 비용 효율성:** **매우 높음.**
  - **장점:** 암호화 과정에서 **애플리케이션 코드 수정이 0%**입니다 (투명성).
  - **비용 발생 지점:** DB 서버 부하를 줄이기 위한 작업 시간 계획, `ALTER TABLE` 실행 시간(I/O 시간)만 고려하면 됩니다.

### B. 컬럼 레벨 암호화 (저효율 경로)

- **방식:** 특정 칼럼의 데이터만 암호화 함수(`AES_ENCRYPT`)를 사용하여 암호화합니다.
- **시간 및 비용 효율성:** **매우 낮음.**
  - **단점:** 데이터베이스와 통신하는 **모든 레거시 서비스 코드(API, 배치 등)**를 찾아 복호화(`AES_DECRYPT`) 로직을 추가해야 합니다.
  - **비용 발생 지점:** **대규모 개발 및 QA(품질보증) 시간**이 발생합니다. 이는 수개월 이상의 기간과 많은 개발 인력을 요구할 수 있습니다.

> 💡 **권고:** 레거시 서비스의 경우 **TDE를 최우선으로 검토**하여 레거시 코드를 건드리지 않는 것이 가장 효율적입니다.

---

## 2. 기존 레거시 데이터 암호화 마이그레이션 평가

기존 데이터 마이그레이션에 드는 **시간(Time)**과 **비용/효율성(Cost/Efficiency)**은 선택한 방식에 따라 아래 요인들이 결정합니다.

### A. TDE를 통한 기존 데이터 암호화 (Alter Table 방식)

TDE로 암호화된 기존 테이블의 데이터 마이그레이션 과정은 **전적으로 DB의 I/O 성능**에 달렸습니다.

#### 1. 시간 및 성능 요인 (Time)

- **총 데이터 크기 (Volume):** 가장 중요한 요소입니다. 암호화되지 않은 기존 **테이블의 총 용량**이 클수록 `ALTER TABLE` 완료 시간이 비례하여 늘어납니다.
- **디스크 I/O 속도:** 스토리지가 SSD인지 HDD인지, RAID 구성은 어떤지에 따라 `ALTER TABLE`의 속도가 결정됩니다.
- **DB 서버 부하:** 암호화 과정은 CPU를 사용하며, I/O를 집중적으로 사용합니다. 트래픽이 낮은 시간대(Down Time)를 확보하거나, MariaDB의 **온라인 DDL 기능**을 사용하여 서비스 중단을 최소화해야 합니다.
  - _TDE 마이그레이션 과정:_ 기존 데이터를 읽어 메모리에서 암호화한 후, 새로운 테이블스페이스 파일에 기록합니다. 이 과정 전체의 시간이 소요됩니다.

#### 2. 비용 및 효율성 (Efficiency)

| 항목          | 평가          | 설명                                                   |
| :------------ | :------------ | :----------------------------------------------------- |
| **인건비**    | **매우 낮음** | DB 관리자(DBA)의 작업 시간만 발생 (개발자 인력 불필요) |
| **QA 비용**   | **거의 없음** | 데이터의 입출력 방식이 동일하므로, 기능 테스트 불필요  |
| **운영 효율** | **높음**      | 운영 중 데이터 파일만 보호되므로, 운영 편의성 유지     |

---

### B. 컬럼 암호화를 통한 기존 데이터 암호화 (스크립트 방식)

컬럼 암호화를 선택하는 순간, 마이그레이션은 DB 작업이 아닌 **소프트웨어 개발 프로젝트**가 됩니다.

#### 1. 시간 및 성능 요인 (Time)

- **개발 시간:** 암호화 대상 칼럼을 읽고, 암호화 함수를 적용하여 다시 쓰는 **마이그레이션 스크립트** 개발 시간이 필요합니다.
- **서비스 통합 시간:** 레거시 서비스 내 모든 쿼리/함수에 복호화 로직을 심는 **개발/통합 시간**이 가장 큽니다.
- **QA 시간:** 모든 서비스 기능에 대한 **보안 및 기능 회귀 테스트(Regression Test)** 시간이 필수적으로 발생합니다. (매우 큼)

#### 2. 비용 및 효율성 (Cost/Efficiency)

| 항목          | 평가          | 설명                                                                        |
| :------------ | :------------ | :-------------------------------------------------------------------------- |
| **인건비**    | **매우 높음** | 마이그레이션 스크립트 개발, 애플리케이션 개발/통합 인력, QA 인력 비용 발생  |
| **QA 비용**   | **매우 높음** | 암호화/복호화 오작동으로 인한 데이터 유실/노출 위험이 높아 QA 시간이 막대함 |
| **운영 효율** | **낮음**      | 키 관리를 애플리케이션이 직접 해야 하므로, 운영 복잡성이 증가               |

## 💡 요약 및 다음 단계

레거시 DB의 부분 암호화 목표를 **가장 빠르고 저렴하게** 달성하려면 **TDE를 통한 테이블 단위 암호화**를 선택해야 합니다.

다음 단계에서는 **암호화 대상 테이블의 총 크기**와 **DB 서버의 I/O 속도**를 파악하여 `ALTER TABLE`에 소요될 시간을 구체적으로 예측해 보시는 것을 추천합니다.
